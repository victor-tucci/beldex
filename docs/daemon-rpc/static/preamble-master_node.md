# Master Node Administration Endpoints

These endpoints allow administering and controlling an active master node.  They generally require
the queried beldexd is running as a master node, and require administrator access.